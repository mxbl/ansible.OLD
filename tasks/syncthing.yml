# vim:ft=yaml.ansible
---
# - name: Add syncthing GPG key
#   tags: syncthing
#   become: true
#   ansible.builtin.apt_key:
#     url: '{{ packages.syncthing.keyfile }}'
#     keyring: '{{ packages.syncthing.keyring }}'
#
# - name: Add syncthing apt repository
#   tags: syncthing
#   become: true
#   ansible.builtin.apt_repository:
#     repo: |
#       deb [ signed-by={{ packages.syncthing.keyring }} arch=amd64 ] {{ packages.syncthing.repo }}
#     state: present

- name: Install Syncthing
  tags: syncthing
  become: true
  ansible.builtin.package:
    update_cache: true
    name: syncthing
    state: present
